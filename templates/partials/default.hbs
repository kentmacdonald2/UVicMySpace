<!doctype html>
<html>
<head>
    <title>{{title}} | {{page.title}}</title>
    {{> meta}}
    {{> imports}}
</head>
<body>
{{> uvicNav}}
{{> uvicHeader}}

<div class="mypage">
    <div class="container">
        <div id="content">
            {{> nav}}
            <div class="row">
                <div class="col-xs-12">
                    {{> subnav}}
                </div>
            </div>
            {{> @partial-block }}
        </div>
    </div>
</div>

{{> uvicFooter}}
</body>
</html>
